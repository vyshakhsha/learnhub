import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#465B79",
    },
    secondary: {
      main: "#7FA1C3",
    },
    tertiary: {
      main: "#E2DAD6",
    },
    bgColour: {
      main: "#F5EDED",
    },
  },
});
export default theme;
